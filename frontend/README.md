# Dev4All — Web (Frontend)

Reserved for the **web client** (planned stack per root `README.md`: React, Vite, TypeScript, Tailwind CSS).

## Status

No application scaffold yet. Add the project here when ready (e.g. `pnpm create vite` or your chosen template).

## Conventions (when initialized)

- Keep environment-specific config out of Git (use `.env.example` only).
- Call the backend API via the documented OpenAPI / HTTPS base URL.

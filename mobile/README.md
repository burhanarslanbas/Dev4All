# Dev4All — Mobile

Reserved for the **native mobile app** (planned stack per root `README.md`: Kotlin, Android).

## Status

No application scaffold yet. Add the Android (or multi-platform) project here when ready.

## Conventions (when initialized)

- Do not commit signing keys, keystores, or `local.properties` with secrets.
- Prefer API base URL and feature flags via build flavors or secure runtime config.
